{
  "name": "webp-converter",
  "version": "0.0.1",
  "description": "Client-side WebP image converter with resizing capabilities",
  "main": "index.js",
  "scripts": {
    "build:wasm": "chmod +x build-wasm.sh && ./build-wasm.sh",
    "build": "npm run build:wasm && mkdir -p dist && cp *.html *.css *.js *.wasm dist/"
  }
}
